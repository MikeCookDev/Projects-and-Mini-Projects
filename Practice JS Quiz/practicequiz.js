var answer = prompt('What was the first Pokemon ever created?');
if ( answer.toUpperCase() === 'RHYDON' ) {
	document.write("<p>Awesome! That's right!</p>");
} else {
	document.write ("<p> Sorry, that's wrong. Please try again.</p>");
}